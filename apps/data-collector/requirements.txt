confluent-kafka>=2.5
minio>=7.2
psycopg[binary]>=3.2
requests>=2.32
prometheus-client>=0.20
# Valve перешли на zstd для реплеев (обнаружено 2026-07-31, патч 7.41),
# расширение в URL при этом осталось .dem.bz2 — формат определяется по магии.
zstandard>=0.22
# libs/manta_grpc импортирует grpc (mTLS-обвязка внутренних вызовов), а
# точка входа коллектора импортирует manta_grpc. До спринта 143 этой
# строки не было: дома коллектор запускается процессом, где grpcio уже
# стоит от соседних сервисов, а в образе его не оказалось — все семь
# коллекторов падали на старте с ModuleNotFoundError и перезапускались по
# кругу.
grpcio>=1.60

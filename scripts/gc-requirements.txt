# Клиент Steam/GC для замера солей реплеев (scripts/gc-probe.py).
#
# Ставится в ОТДЕЛЬНЫЙ venv вне репозитория (make gc-venv): dota2 тянет
# protobuf 3.20 и gevent, а коллекторы и gRPC-сервисы живут на своём
# protobuf. В один интерпретатор это не помещается.
steam==1.4.4
dota2==1.1.0

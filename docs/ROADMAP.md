# Manta — сводный роадмап оставшихся работ

Обновлено: 2026-07-18 (после спринта 31). Источники: спецификация Гл. 14,
анализ методики обучения (2026-07-18), бэклог Фазы 3–4.

**Текущее состояние**: полный конвейер жив (реплеи + гибридный JSON-сбор),
1086 матчей, автообучение с честным гейтом и PSI-дрейфом, SHAP в отчётах,
реестр S3 + опциональный MLflow, Brier валидация 0.1532 (порог спеки ≤ 0.18).

---

## Трек A — качество модели Win Probability

Из анализа методики; нумерация сохраняет приоритет «профит/усилие».

| # | Работа | Статус / заметки |
|---|---|---|
| A1 | ~~Гибридный JSON-сбор + NaN для пропусков~~ | ✅ спринт 31 (×5–10 к притоку; /parsedMatches, а не /publicMatches — свежие паблики не распаршены) |
| A2 | **Фичи `alive_diff`, `towers_diff`/`rax_diff`** | Следующий спринт. Реплеи: PositionSnapshots.is_alive + combat log (проверить, что парсер не фильтрует смерти строений). JSON-матчи: башни из `objectives`, живые герои недоступны → NaN |
| A3 | **OOF-калибровка (GroupKFold 5 по матчам)** | Закрывает двойное использование валидации (early stop + калибратор + гейт). OOF-Brier в артефакт как честная метрика. Финальный бустер на всех данных, num_rounds = медиана best_iteration |
| A4 | Веса строк `1/n_rows(match)` | Длинные матчи не должны доминировать в лоссе |
| A5 | Platt вместо изотоники при < 50 матчах | Фиксировать выбор калибратора в метаданных |
| A6 | `networth_rel = networth_diff / networth_total` | Требует поля networth_total в витрине (есть в JSON и в EconomyTimeline) |
| A7 | Фазовые Brier-метрики (0–10 / 10–25 / 25+ мин) | В артефакт, Telegram-нотификацию и дашборд; гейт остаётся на агрегате |
| A8 | Про-эталон: возобновить приток Professional | Витрина 100% Premium — релизный гейт по pro стоит. JSON-путь работает и для /proMatches (лиги всегда распаршены) — дешёвый способ набрать эталон |
| A9 | Даунвейт старого патча после баланс-патча | Вес ×0.3–0.5 вместо выбрасывания; PSI-триггер уже ловит смену меты |
| A10 | Roshan/aegis, buyback, hero-фичи, Optuna-тюнинг | После стабилизации A2–A7 и 5 000+ матчей |
| A11 | Гиперпараметры под малые данные (num_leaves 15–20, min_data_in_leaf 50+, lambda_l2) | Вместе с A3, чтобы мерить честно |

## Трек B — релиз Win Probability

Критерии из анализа (§5.1): порог — 1 000 матчей текущего патча (пройден),
плюс два качественных гейта.

| # | Критерий | Статус |
|---|---|---|
| B1 | OOF-Brier ≤ 0.18 | Ждёт A3 (текущие 0.1532 — на одном сплите, оптимистичны) |
| B2 | Brier про-эталон ≤ 0.20 на ≥ 30 pro-матчах | Ждёт A8 |
| B3 | Beta-метка WP-кривой на фронте | Можно сразу — данных достаточно |
| B4 | Публичный релиз WP | После B1 + B2; после патчей — 200–300 матчей новой меты на переобучение |

## Трек C — крупные фичи спеки (Фаза 3–4)

| # | Работа | Заметки |
|---|---|---|
| C1 | **Similarity Engine** (Гл. 3; proto `SimilarityService` готов) | Эмбеддинги матчей из витрин, точный косинус-поиск (ANN не нужен на этом масштабе), `FindSimilar`/`RetrieveContext` — фундамент RAG |
| C2 | **Draft Engine — бейзлайн** | Статистика синергий/контрпиков по hero_id из PlayerMatchFeatures; GNN из спеки — при 10 000+ матчей |
| C3 | **LLM-Coach / RAG-каркас** | Шаблонный текстовый генератор поверх RetrieveContext + error/impact/SHAP полей отчёта; реальный LLM подключается env-ключом позже |
| C4 | Feature Store — онлайн-слой (Redis) | Для live-инференса (Гл. 3.6); proto `FeatureStore` готов |
| C5 | Laning/Error-модели поверх baseline ΔWP | Требуют реплейных фич (позиции) — реплей-путь сбора сохраняется ради них |
| C6 | Frontend-развитие | Драфт-симулятор, карта, показ SHAP-драйверов у ошибок (данные в отчётах уже есть — спринт 29) |

## Трек D — MLOps и эксплуатация

| # | Работа | Заметки |
|---|---|---|
| D1 | Продовый MLflow | Бэкенд готов (спринт 30, REGISTRY_BACKEND=mlflow); переключение + postgres-URI вместо sqlite |
| D2 | Авточистка старых версий моделей в реестре | ~4 версии/день × навсегда = мусор; хранить последние N + все продвигавшиеся |
| D3 | CI: тесты data-collector и report-generator | Сейчас в workflow только ml-service |
| D4 | Runbooks инцидентов | «Витрина не растёт», «гейт всё отклоняет», «PSI-алярм» — по мотивам реальных случаев из этого чата |
| D5 | Нагрузочные тесты NFR-PERF/SCAL | Гейт Фазы 4 |
| D6 | Security review (SAST/SCA, секреты, GDPR) | Гейт Фазы 4; из практики: секреты уже вынесены в env, `.env` в gitignore |

## Трек E — окружения

| # | Работа | Заметки |
|---|---|---|
| E1 | Автозапуск стека на локалке (Windows) | Планировщик задач → `wsl … make recover` + автозапуск Docker Desktop; вариант — systemd в WSL |
| E2 | Синхронизация облако↔локалка | Сейчас — дампы витрины вручную; когда локалка стабильна, облачный сбор можно остановить, чтобы не плодить расхождение |

---

## Предлагаемый порядок ближайших спринтов

1. **Спринт 32 — A2**: `alive_diff` + `towers_diff`/`rax_diff` (главный прирост качества; extractor + parser-проверка + JSON objectives).
2. **Спринт 33 — A3 + A11 (+A4, A5)**: OOF-калибровка, веса строк, Platt-переключатель, гиперпараметры малых данных. После него B1 измерим честно.
3. **Спринт 34 — A8**: pro-эталон JSON-путём (/proMatches) → B2, релизное решение по WP (B4).
4. **Спринт 35 — A6 + A7**: networth_rel + фазовые Brier.
5. **Спринт 36+ — C1 → C2 → C3** (одобренный бэклог «все три по порядку»), параллельно мелкие D2/D3.

Пункты 1–3 закрывают ~90% достижимого прироста качества при минимуме кода
(вывод анализа подтверждён живыми проверками спринта 31).

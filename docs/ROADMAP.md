# Manta — сводный роадмап оставшихся работ

Обновлено: 2026-07-26 (после спринтов 60–69 + подъём темпа сбора). Источники:
спецификация Гл. 14, анализ методики обучения (2026-07-18), бэклог Фазы 3–4,
`docs/HANDOFF.md` (хроника инцидентов и договорённости с владельцем).

**Текущее состояние**: полный конвейер жив (реплеи + гибридный JSON-сбор),
автообучение с честным гейтом и PSI-дрейфом, SHAP в отчётах, реестр S3 +
опциональный MLflow (постгрес-бэкенд), Win Probability релизные критерии
B1+B2 пройдены, Death-Risk модель (спринт 48) заменила эвристический Safety
Index в отчётах, шардированный сбор на нескольких машинах без дублей.
Облачный сбор остановлен (решение владельца) — локалка (Windows/WSL2) —
единственный источник истины для данных и обучения.

---

## Порядок работ, установленный владельцем (2026-09-07)

Четыре блока, строго по приоритету. Смысл порядка: **сначала система
должна уметь работать без присмотра**, потому что всё остальное строится
на данных, которые она соберёт, и на модели, которой можно доверять. Пока
модель ведёт себя непредсказуемо, а сбор даёт десятки матчей в сутки,
любая работа над сайтом и «философией» опирается на песок.

### Блок 1 — работоспособность модели и объём сбора (первоочередной)

Цель блока: **оставить систему собирать матчи и не возвращаться к ней**.
Готовность признаётся не по закрытым пунктам, а по двум числам: приток
матчей держится на заданном уровне неделю, и ни одно продвижение модели
не потребовало ручного разбора.

*Модель.*

| # | Работа | Что известно на 07.09 |
|---|---|---|
| G1 | **Храповик гейта** | ✅ спринт 207. Гейт сравнивал кандидата ТОЛЬКО с текущим prod, и планка пересчитывалась от неё же. За 07.09 про-эталон prod прошёл 0.1553 → 0.1565 → 0.1565 → 0.1578: каждый шаг «в пределах шума» (σ≈0.0009), сумма +0.0025. Якорь — **сам артефакт лучшей версии** (стейдж `champion`), а не запомненное число: Brier'ы разных запусков посчитаны на РАЗНЫХ данных, потому что про-эталон пересобирается с приходом новых про-матчей, и сравнение сохранённых чисел не различало бы «модель хуже» и «выборка труднее». Планка движется только вверх и только по доказанному улучшению |
| G2 | **Решает один holdout, остальные справочные** | ✅ спринт 214. Решено не «согласием обоих» и не «расхождение = отказ», а по существу: **решает выборка свежих матчей** — единственная, свободная и от утечки, и от перетасовки эталона (её не видел ни один участник сравнения), при размере ≥50 матчей. Про-эталон стал **сторожем от обвала** (0.01 Brier — на порядок больше допуска), а не судьёй: решение владельца 10.09 — продукт это публичные матчи, отчёты делаются для собранных, а они почти все публичные. Мягкость сторожа не от доброты: пока обе стороны сравнения не обучены после спринта 213, про-оценка систематически льстит той, что старше. Граница «свежести» считается по ОБОИМ участникам (после отката якорь может быть обучен позже прода). Объяснение теперь ГОВОРИТ, какая выборка решила: пока решала всегда первая, это означал порядок строк |
| G3 | **Смещённое сравнение не называет себя смещённым** | Анти-утечковый фильтр `fresh` требует ≥30 свежих матчей и молча откатывается к общей валидации. Отчёт в обоих случаях выглядит одинаково |
| G4 | **Малый эталон** | Про-эталон 663–670 матчей, и он почти не растёт. Пока он не растёт, каждое переобучение сравнивается со всё более старой выборкой |

*Сбор.*

| # | Работа | Что известно на 07.09 |
|---|---|---|
| C1 | **Доля потока достаётся источнику, который не может собирать** | Деление публичного потока включается по ФАКТУ НАЛИЧИЯ `STRATZ_API_TOKEN` (`__main__.py:148`), а не по его пригодности. Токен недействителен (403 каждый цикл, сутки подряд), контейнер при этом `Up`, и половина кандидатов не собирается никем. Источник обязан терять долю, как только перестаёт собирать |
| C2 | **Поднятый потолок отключил гарантии долей** | На VPS `OPENDOTA_GLOBAL_LIMIT` был уже 15000, то есть в бюджет сбор НЕ упирался (умолчание 1900 — дефект для новой машины, но не причина здешнего простоя). Беда в другом: `SHARES` остались абсолютными числами с суммой 1900. Резерв под чужие гарантии считается от них, поэтому утром источник мог взять 13350 из 15000 — гарантии превратились обратно в «кто успел», ровно то, ради предотвращения чего модуль бюджета и написан (простой 2026-08-06, шесть часов). Доли обязаны следовать за потолком |
| C3 | **Доктор смотрит не в тот счётчик** | `doctor.sh:115` берёт `remaining-day` из БЕЗ-КЛЮЧЕВОГО запроса к `/api/health`, то есть меряет квоту анонимного тарифа для IP. Коллекторы ходят с платным ключом — это другое ведро. 07.09: доктор показал `remaining-day=2999`, `ApiBudget` — 1771 потраченный вызов. Число не бесполезно, а успокаивающе: показывает запас там, где его не мерили. Смотреть надо в `ApiBudget` и `OPENDOTA_GLOBAL_LIMIT` |
| C4 | **Хранение удаляет неразобранное** | Lifecycle-правило на бакете удаляет .dem по возрасту (`REPLAY_RETENTION_DAYS`, 3 суток) с пустым префиксом и без связи с разбором. Авария 3–6 сентября длилась ровно этот срок и уничтожила собственные улики: из 510 сирот 262 потеряны навсегда. Варианты: (а) сторож считает сирот и кричит на росте — тогда трёх суток хватает, потому что узнают в тот же день; (б) срок до 7 суток ценой ~втрое большего диска. Вариант (а) лечит причину |
| C5 | **Никто не проверяет, что изменённый код доехал до сервиса** | Спринт 195 правил `timeline_runner.py`, а раскатывали `data-collector` — сервис, который исполняет `runner.py`. Четыре сервиса JSON-пути (`timeline-collector`, `pro-timeline-collector`, `league-collector`, `stratz-collector`) остались на старом образе, и это не проявлялось ничем: новый код просто не выполнялся. Нужна проверка соответствия «файл → сервис, который его исполняет» |
| C6 | **Успешная публикация не оставляет следа** | `_announce` логирует только ошибку, поэтому «доставлено» и «код не выполняется» выглядят одинаково — пустым grep'ом. Нужен счётчик отправленных событий, а не запись в лог на каждый матч |

**Сколько матчей вообще достижимо (замер 07.09.2026).** Семь циклов
`timeline-collector`: 196 осмотров кандидатов, собрано 4 матча — выход
**2%**, то есть ~49 вызовов на матч. Ранг и режим известны только ПОСЛЕ
`/matches/{id}`, поэтому мы платим и за отвергнутых; главный отсев не
ранг (4 из 28 за цикл), а Turbo и событийные режимы (10–22 из 28).
Фильтр режимов верный и смягчению не подлежит: Turbo — другая экономика.

| цель, матчей/сут | вызовов/сут | $/мес |
|---|---|---|
| 300 (объявленный бюджет) | 14 700 | 44 |
| 1 000 | 49 000 | 147 |
| 8 000 | 392 000 | 1 176 |
| 15 000 | 735 000 | 2 205 |

Вывод: **15 тысяч в сутки этим путём не берутся** — не из-за настроек, а
потому что мы платим за осмотр кандидата, которого отвергнем. Дорога
туда лежит не через деньги, а через отбор НА СТОРОНЕ ИСТОЧНИКА: листинг,
умеющий фильтровать по рангу и режиму до оплаты. Ради этого в проекте и
заводился STRATZ с его GraphQL — вернуть его дешевле, чем платить
двадцатикратно за осмотры вслепую. Отдельный кандидат — OpenDota
`/explorer` (SQL-выборка на их стороне).

Побочные наблюдения того же замера:

* **2–12 из 28 осмотров кончались ошибкой запроса** — до 40% оплаченного
  бюджета. ✅ спринт 197: постоянная ошибка (400/404/410) теперь
  отправляет кандидата в кэш отказов, как и отказ фильтра, а временная
  (5xx, обрыв) — нет. Раньше матч, которого у OpenDota нет, осматривался
  заново каждые полчаса, пока не уползёт из окна листинга. Отчёт цикла
  называет код, так что следующий замер скажет, сколько из этих 40% было
  постоянными.
* **Половина циклов возвращает ноль кандидатов** — листинг не отдаёт
  нового. Не разобрано: то ли `skip_freshest`/`id_lag` смещают окно мимо
  свежих матчей, то ли `/parsedMatches` действительно пуст на этом
  интервале. Мерить до того, как крутить настройки.

### Спринты 199–205 — то, чего в блоках не было (план 08.09.2026)

Семь работ, выведенных из разбора недели 02–08 сентября. Порядок — по
отношению пользы к стоимости: сначала замеры, которые почти ничего не
стоят и могут переставить приоритеты, потом дорогое.

| # | Работа | Почему |
|---|---|---|
| **199** | **Сверка `tier` с наблюдаемым рангом** | ✅ Ранг проведён по всей цепочке (у реплейного пути его не было вовсе — 85% данных не сверялись ни с чем); `make tier-audit` разводит три диагноза: ранг неизвестен, ранг низок, уровень высокого не обещал. Уровень матча — КОНСТАНТА источника (`salts`/`candidates` → Pub, `opendota_public` → Premium), и никто не сверял её с действительностью. При этом `tier` делит датасет на обучение и про-эталон: систематическая ошибка портит не данные, а ГЕЙТ, и делает это молча. Проверить стало можно — `avg_rank` лежит в витрине с миграции 018. Замер, а не спринт по объёму, но последствия у него модельные |
| **200** | **Потолок реплейного пути** | ✅ Перечень источников — из `CollectorCursor` (первым же пропавшим со страницы оказался `candidates` с 58% бюджета); `last_cycle_at` отличает «молчит» от «его нет»; в докторе появился сторож диска с прогнозом по ФАКТИЧЕСКИ собранным реплеям. Живой замер 08.09: реплейный путь даёт 82 матча в сутки, JSON-овый — 14. Две недели работы ушли в JSON-путь как в «основной приток», а он второстепенный. Потолок реплейного НИКТО НЕ МЕРИЛ: диска свободно 70 ГиБ, `salts` даёт 32 в сутки вообще без вызовов OpenDota (соли добывает GC). Возможно, самый дешёвый рост объёма лежит здесь |
| **201** | **Правило «объявление ≠ факт»** | ✅ Таблица из десяти случаев в HANDOFF плюс три производных правила. Механическая часть: раздел доктора обязан что-то измерить (`test_doctor_measures.py`). За неделю восемь раз одна форма ошибки: том смонтирован ≠ в него пишут; контейнер `Up` ≠ источник работает; токен задан ≠ токен годен; образ пересобран ≠ сервис его исполняет; адрес напечатан ≠ адрес отвечает; SQL написан ≠ SQL выполнялся; имя в одной таблице ≠ то же имя в другой; тест написан ≠ тест проверяет поведение. Каждый раз выглядело по-новому и ловилось только на живой машине. Список мест, где спрашивать «а проверено ли?», дешевле любого теста и работает поперёк языков |
| **202** | **История версий модели** | ✅ Миграция 017 `ModelVersions`: решение, объяснение и РАЗОБРАННЫЕ оценки holdout'ов с `brier_prod` — тем самым, по которому только и виден храповик. Пишутся все оценки и отклонённые версии тоже. Журнал не мешает промоушену. Решения гейта живут ТОЛЬКО в Telegram. Храповик (G1) стал виден лишь потому, что владелец прислал четыре сообщения подряд. Без таблицы нельзя ни построить график, ни поднять алерт на НАКОПЛЕННОЕ ухудшение, ни ответить «когда prod стал хуже». Попутно даёт содержимое админской странице блока 3 |
| **203** | **Сквозной тест конвейера** | ✅ `make smoke`: настоящий свежий матч → событие → сдвиг отметки отчёта. Без синтетики — выдуманный `match_id` уехал бы в обучающую выборку. Всё покрыто модульно, а ВСЕ аварии недели случились на стыках: топика нет — сообщение исчезло; событие не публиковалось; сервис не пересобран; SQL ни разу не выполнялся; имя источника пишется двумя способами. Ни один не мог быть пойман тестом отдельного куска — каждый кусок был исправен. `make smoke`: один синтетический матч сквозь весь путь, отчёт на выходе. Поймал бы пять из семи разобранных дефектов. `FixtureSource` для этого уже есть |
| **204** | **Резервирование матчей между путями** | ✅ Замер показал другую причину: дубликатов 7 за цикл, а 36% ВСЕГО потока не собирает никто, потому что источники не равносильны на общих кандидатах. Миграция 018 `SourceDeclines`: отвергнутое напарником берётся сверх своей доли. STRATZ берёт кандидатов из листинга OpenDota, и 70% его вызовов — «нет матча»; дедуп срабатывает ПОСЛЕ оплаты. Кто первый заявил матч — тот и берёт. Вернуло бы большую часть его квоты |
| **205** | **Один источник списка маршрутов** | ✅ Три списка сверяются между собой. Найдено четыре призрака: `/meta/heroes` вёл на 404, ещё три маршрута описаны, но не отдаются — помечены `x-manta-status: planned` с причиной. Пять публичных маршрутов записаны дважды: `PublicRoutes` на Go и регулярками в `apps/manta-site/app/api/manta/[...path]/route.ts`. Разъедутся молча — маршрут появится в API, а прокси отобьёт его 404, и искать будут в API. `openapi/manta.yaml` уже есть; из него список и порождать |
| **206** | **Сторож проверяет свой собственный вход** | ✅ `make smoke` дважды подряд сообщал «отчёт не появился за 300с» при **полностью исправном конвейере**: продюсер звался через `docker exec` **без `-i`**, стандартный ввод контейнеру не отдавался, сообщение не публиковалось вовсе. Отказ невидим по всем обычным признакам — код возврата нулевой, stderr пуст, лаг консьюмер-группы нулевой (у непроизведённого сообщения он такой же), а генератор в те же минуты производил отчёты по другим матчам. Диагноз уводил в сторону тем вернее, чем внимательнее его читали. Исправлено трижды: `-i`; **сверка конечных смещений топика до и после** — публикация теперь измеряется, а не объявляется, и её отсутствие называется сразу, а не через таймаут; правило по всем `scripts/*.sh` (`test_docker_exec_stdin.py`), потому что 34 из 36 обращений к `docker exec` ввод не читают, и форма без `-i` привычна глазу. Прогон впервые исполняется тестом целиком — на подставных `docker` и `curl`, где подставной `docker` воспроизводит настоящее поведение: без `-i` ввода нет. Ни один статический тест этого поймать не мог: в тексте были и продюсер, и топик, и сравнение отметок. Это **третий случай «объявление — не факт» за неделю** — и первый, где так соврал сам сторож |
| **207** | **Храповик гейта (G1 блока 1)** | ✅ Кандидат обязан быть не хуже **якоря** — лучшей известной версии, — а не только текущего прода. Без этого серия ничтожных по отдельности ухудшений проходит беспрепятственно: 0.1553 → 0.1565 → 0.1565 → 0.1578 за сутки, каждый шаг в пределах σ≈0.0009, итог +0.0025 — почти втрое больше допуска, который каждый шаг проходил. **Якорь — артефакт, а не число**: Brier'ы разных запусков посчитаны на разных данных (эталон пересобирается с приходом про-матчей), и сравнение сохранённых чисел не различало бы «модель стала хуже» и «выборка стала труднее» — ровно та ошибка, от которой честный гейт избавлялся в спринте 122. Обе модели считаются СЕЙЧАС и на одном holdout, поэтому якорь не протухает: устарел он на самом деле — кандидат его обойдёт и займёт место. Планка (стейдж реестра `champion`, защищён от `cleanup` историей промоушенов) движется **только вверх и только по доказанному улучшению**: подними её на ничью — и храповик завёлся бы заново, с лишним шагом. Недоступный якорь не морозит обучение и **говорит об этом** («храповик не применён») — отсутствие ≠ ноль. Отказ по якорю идёт в объяснении ПЕРВЫМ: «не хуже prod, но отклонён» без причины выглядит поломкой гейта. Десять мутаций, все пойманы; главный тест — не наличие сравнения, а остановленная ЦЕПОЧКА |
| **208** | **Инструменты бегут там, где стоят их зависимости** | ✅ `make ml-status` на VPS упал на `ModuleNotFoundError: No module named 'minio'`: цель гоняла ХОСТОВЫЙ python3 против кода приложения, чьи зависимости стоят только в образе. На машине владельца окружение с ними было, поэтому за сотню прогонов не всплыло — вскрылось на единственной оставшейся машине, в момент, когда инструментом надо было воспользоваться. Та же беда, что в спринте 143 (`pg-migrate.sh` звал хостовый psql), и правильный образец опять лежал рядом: эти модули крутятся в контейнерах круглосуточно. Двенадцать целей переведены на `scripts/in-image.sh`; **чтение и обучение — в разные контейнеры**, потому что у ml-service ключ реестра только на чтение и лимит 768 МиБ: обучение через exec считалось бы в его cgroup, и OOM убил бы не обучение, а сервис инференса вместе с генерацией отчётов. `tools/` добавлены в образы коллектора и feature-extractor — цена уже платилась вручную, через `docker cp`. Подмены хостом нет намеренно: откат вернул бы тот же отказ, только молча. Правило по всему Makefile с поимённым списком исключений (запуск сервиса руками, тесты, кодогенерация) — и **мутация «откатиться на python3» пережила первую редакцию тестов**: отказ оставался ненулевым, обе строки лечения печатались, а подмена была невидима. Поймана только подставным интерпретатором, доказывающим, что его НЕ звали |
| **209** | **Якорь выбирается, а не подворачивается** | ✅ Изъян собственного спринта 207, найденный на живом выводе `make ml-status`. Якорь заводился на ПЕРВОЙ продвинутой версии, то есть на той, что подвернётся следующей, — а ею по построению оказывается ХУДШАЯ из виденных: планка встала бы ровно там, куда съехал прод, и храповик держал бы деградацию вместо того, чтобы её остановить. Реестр на 08.09: 0.1581 → 0.1572 → 0.1599 → 0.1594 → **0.1617** (production). Теперь сам собой якорь заводится только на ПЕРВОЙ ВЕРСИИ ВООБЩЕ, когда выбирать не из чего; при живой истории выбор — решение, а не побочный эффект промоушена: `make ml-anchor` (`--apply`) скачивает последние версии плюс production и текущий якорь и сравнивает их ЧЕСТНО — все на одном сегодняшнем holdout. Сохранённые в реестре Brier для приговора не годятся (разные данные) и служат только тому, чтобы сузить круг. Пока выбор не сделан, храповик молчит и **говорит об этом** в объяснении гейта, то есть и в Telegram. Битая версия пропускается, а не отменяет выбор. Четыре мутации, все пойманы. **Живой результат**: за часы до раскатки переобучение успело завести якорь на себя — на худшую из восьми версий (0.1622), ровно как предсказано; `--apply` переставил его на лучшую. Честный замер подтвердил деградацию: production хуже лучшей на **+0.0024** на общем holdout из 668 матчей. И порядок версий по честному замеру НЕ СОВПАЛ с порядком по сохранённым числам (лучшей оказалась 0907T083343, а не 0907T023312) — то есть выбор по метаданным дал бы другой якорь |
| **210** | **Планка, которая не кусается, — не планка** | ✅ Храповик отклоняет кандидата при Δ > max(0.001, σ), значит разрыв МЕНЬШЕ σ он не заметит, сколько бы его ни показывали в таблице. `make ml-anchor` печатает σ и допуск и выносит вердикт: РАЗЛИЧИМО или В ПРЕДЕЛАХ ШУМА. Второе — не отговорка, а диагноз: эталон мал для различий этого размера, и лечится это ростом эталона (G4), а не правкой порогов. Присутствие ≠ пригодность: армированный храповик и кусающийся храповик — разные утверждения. **Фикстура теста была неверна с первого раза**: симметричный сдвиг вероятностей даёт НЕсимметричный Brier (квадрат выпуклый), и «неразличимый» случай оказался различим вчетверо — то есть проверял ровно обратное. Построена заново по квадратичной ошибке. Четыре мутации, все пойманы. **Живой замер: σ0.0010 при разрыве 0.0024 — РАЗЛИЧИМО, храповик кусается.** G1 закрыт замером, а не объявлением |
| **211** | **Откат на выбранную версию** | ✅ Из замера 210 следовало неприятное: отчёты считает модель на 2.4σ хуже той, что лежит в реестре, и сама собой ситуация не рассосётся — храповик теперь отклоняет всё, что хуже 0.1608, то есть застрявший прод себя не вылечит. Откатить было нечем: `promote` в реестре есть, но наружу не выходил — production назначал только гейт и только вперёд. `make ml-promote ARGS="<версия> --apply"`. **Ручной путь НЕ отменяет правило гейта**: значимо хуже якоря — отказ с названным числом; `--force` есть (сменилась мета), но требует сказать это вслух и помечает промоушен в выводе. Правило переиспользуется, а не копируется — своя арифметика означала бы разные вердикты на одних числах у ручного и автоматического пути, и тест это стережёт. Шесть мутаций; **одна выжила**: «убрать раннюю проверку версии» — отказ приходил позже, из другого места, и выглядел так же. Поймана утверждением, что несуществующую версию не пошли СКАЧИВАТЬ |
| **212** | **По отчёту видно, какая модель его посчитала** | ✅ Найдено попыткой проверить откат 211: `MatchReports.model_version` хранил `0.9.0` — semver из артефакта, ОДИН И ТОТ ЖЕ у всех 26 версий реестра. То есть строки отчётов до отката и после неотличимы, а вопрос «какие отчёты сделаны съехавшей моделью» — то есть что перегенерировать — задать было нечем. Теперь модель, загруженная из реестра, носит версию реестра, и версия берётся из ТОГО ЖЕ `resolve`, что и веса: отдельный `stage_version` разошёлся бы с загруженными весами при промоушене между двумя обращениями, и отчёт приписали бы не той модели — ошибка, которая не падает, а врёт в поле, по которому потом разбираются. Локальный файл остаётся при semver: «не из реестра» ≠ «версия неизвестна». Четыре мутации; **одна выжила** — «спросить версию отдельным запросом»: тест считал ВЫЗОВЫ, а отдельный опрос идёт в другой метод реестра. Переписан так, чтобы воспроизводить саму гонку. Отчёты, записанные раньше, так и остаются с `0.9.0` — чем их посчитали, уже не восстановить |
| **213** | **Разбиение перестало перетасовываться** | ✅ **Гейт был нечестен, и это объясняет семь отказов подряд 9–10.09.** Эталон и валидация набирались перестановкой списка матчей, а перестановка списка другой длины — другая целиком: замер показал, что **добавление ОДНОГО про-матча перебрасывает 11% эталона** в обучение и обратно, сорока — 24%. Значит, production, обученная несколько дней назад на своей половине про-матчей, меряется сегодня на матчах, часть которых она ЗАПОМНИЛА, — а кандидат обучен на сегодняшнем разбиении и эталона не видел. Гейт систематически льстил проду. Совпадает с логами: на про-эталоне кандидат «значимо хуже» семь раз из семи, а на свежих матчах, которых не видел никто, — не хуже или лучше пять раз из пяти. Побочно объясняется несравнимость чисел: у ЗАМОРОЖЕННОЙ модели Brier эталона гулял 0.1598 → 0.1509 за два дня — менялся эталон, а не модель. Теперь сторона матча берётся хэшем от `match_id`: не зависит ни от размера датасета, ни от состава. Цена названа прямо: доля больше не точная (биномиальный разброс). Пять мутаций; **одна выжила** — общая соль для эталона и валидации давала валидацию из ОДНОГО матча (ранняя остановка мертва, вид обычный), и все прочие проверки это пережили: устойчивое разбиение может быть бессмысленным |
| **214** | **Решает выборка свежих матчей (G2)** | ✅ Прямое следствие 213 и решения владельца 10.09: продукт — публичные матчи, про-эталон — сторож. Решает `fresh` при ≥50 матчах (сейчас 152, растёт на ~100/сут), про ловит обвал ≥0.01 Brier. Разрывает и тупик перехода: prod и якорь обучены по старой схеме, их выборки пересекаются с новым эталоном, поэтому про-оценка ещё некоторое время льстит старшей модели — а `fresh` честна с обоими уже сейчас. Граница свежести берётся по максимуму из prod и якоря. Шесть мутаций, все пойманы. Заглушка `_judge_holdout` в старом тесте отдавала урезанный detail — гейт свалился бы KeyError там, где в бою работает; форма приведена к боевой |

### Блок 2 — сайт

**Релиз наружу пока не нужен** (решение владельца 07.09): нужен
круглосуточный доступ к метрикам и удобное место, где собирать фронт.
Это снимает домен и TLS с критического пути — публичного сайта нет,
значит нет и требований, которые к нему прилагаются.

| # | Работа | Заметки |
|---|---|---|
| S1 | **Tailscale как способ доступа** | Частная сеть вместо публикации: VPS и устройства владельца получают адреса, метрики и API доступны круглосуточно с телефона и ноутбука, при этом наружу не открыт ни один порт. Домен, сертификаты и basic-auth не нужны вовсе — а значит, нечему протухать и нечего защищать. Альтернатива (`sslip.io` + Let's Encrypt + basic-auth) хуже: пароль в открытом интернете и сертификат, который надо продлевать |
| S2 | **Фронт против живого API** | Ничего настраивать не требуется: `http://localhost:5173` уже в `PUBLIC_API_CORS_ORIGINS`, обе стороны по HTTP, смешанного контента нет. До S1 достаточно ssh-туннеля на 8081 |
| S3 | **Недостающие маршруты** | Мета, игроки, похожие матчи — то, что владелец перечислял как «потом» |
| S4 | **Фронтенд под готовый контракт** | Пять маршрутов спринта 192 + обязательный учёт `available` (спринт 195): «данных нет» и «данные нулевые» рисуются по-разному, `final_radiant_wp` бывает `null`, пагинация курсором |
| S5 | **Домен и TLS** | Отложено до решения о публичном релизе. Тогда: A-записи, `api.mantaml.com`, certbot, nginx вместо нынешнего 80-го порта |

### Блок 3 — метрики VPS, только для админов

Показать то, что сейчас видно лишь через `make doctor` и Telegram:
свежесть витрины, приток матчей, расход бюджета API, состояние
конвейера, последние решения гейта. Доступ — через Tailscale (S1), то
есть отдельной авторизации не требуется: в частной сети посторонних нет.

Разумно делать ПОСЛЕ блока 1 по двум причинам. Половина полезных метрик —
это ровно те счётчики, которых там сейчас не хватает (C3, C6). И вторая:
доктор обязан сверять ЖИВУЮ конфигурацию с целью по притоку — тест в
репозитории стережёт умолчания compose, а машина живёт по
`~/manta-train.env` и переопределяет их (замер 07.09: `TIMELINE_LIMIT=14`
против умолчания 70). Расхождение боевой настройки с целью не видно
ниоткуда, и это дыра того же рода, что и все прочие в блоке 1.

### Блок 4 — «философия» работы модели

Углублённая работа над тем, ЧТО модель считает и почему: трек F
(`docs/ML-PLAN.md`), hero embeddings (F8, гейт 20K матчей), C10–C12
(поминутный слайдер, модель маршрутов фарма, оценка вардов). Начинать
только когда появятся понятные результаты — то есть после блока 1 и
накопленного объёма.

---

## Трек A — качество модели Win Probability

Из анализа методики; нумерация сохраняет приоритет «профит/усилие».

| # | Работа | Статус / заметки |
|---|---|---|
| A13 | ~~Каталог игровых переменных для объяснимости~~ | ✅ спринт 82: `docs/SIGNALS-CATALOG.md` — 25 кандидатов (драки и расстановка, контроль карты, экономика и тайминги, роли) с оценкой ценности, сложности и доступности данных. Главный вывод: почти всё интересное живёт только в реплеях, а их 103 из 1230 матчей — приток реплеев важнее самих фич. Единица обучения для модели драк — драка, а не матч, поэтому нужна ~1000 реплейных матчей, а не миллионы |
| A14 | ~~Интеграция волны 1: локальный перевес и собранность~~ | ✅ спринт 83: `local_manpower_diff` (перевес В ТОЧКЕ КОНТАКТА: пары «свой-чужой» ближе 1200 юнитов, участники в радиусе 1800 от центра стычки) и `spread_diff` (разброс команды до центра масс). Закрывают слепое пятно `alive_diff`, который считал живых по всей карте и не отличал «пятеро кучей» от «пятеро по карте». Миграция 014, feature_version 1.5.0, монотонность +1 у перевеса и 0 у разброса (кучность выигрывает драку, но в фарм-фазе теряет карту). Только из реплея — у JSON-матчей NaN. ✅ спринт 84: `MatchFights` (миграция 015) — агрегат драк пишется из feature-extractor и переносится слепком. Осталось из плана волны 1: покрытие вижна и доступность бэйбека. Каждая проходит `make ml-ablation`; не давшая значимого улучшения — удаляется. Блокер: `ReplayEvents` живёт 14 дней, поэтому таблицу драк надо завести ДО накопления, иначе история стирается |
| A12 | ~~Про-домен в обучении + симметричная калибровка~~ | ✅ спринт 80: замер 2026-07-31 — skill 0.58 на пабликах против 0.27 на про при равной неопределённости. Причина не приор (смещение +0.035, вклад в Brier <2%), а то, что про-tier целиком был holdout'ом: модель не видела домен, на котором её меряет гейт, а про-строк в датасете было больше, чем паблик-строк. `PRO_TRAIN_FRAC` (0.5) делит про по матчам между train и эталоном; калибратор учится и на зеркальных парах (`algo ...+symcal`). Эталон уменьшился 556 → ~278 матчей — исторические Brier в реестре считались на другой популяции. **Результат**: 0.8.0-20260731T071333Z promoted, эталон 0.1824 → 0.1682 (гейт: Δ−0.0053 при σ0.0025), смещение на про +0.035 → +0.008; плата — паблики 0.1034 → 0.1084 |
| A1 | ~~Гибридный JSON-сбор + NaN для пропусков~~ | ✅ спринт 31 (×5–10 к притоку; /parsedMatches, а не /publicMatches — свежие паблики не распаршены) |
| A2 | ~~Фичи alive/towers/rax~~ | ✅ спринт 32: WP 0.5.0, первое статистически значимое улучшение (Δ−0.0094 при σ0.0030) |
| A3 | ~~OOF-калибровка~~ | ✅ спринт 33: WP 0.6.0 + fresh-holdout гейт (max_match_id) |
| A4 | ~~Веса строк~~ | ✅ спринт 33 |
| A5 | ~~Platt при < 50 матчах~~ | ✅ спринт 33 |
| A6 | ~~networth_rel~~ | ✅ спринт 35: производная при загрузке, NaN для строк без total |
| A7 | ~~Фазовые Brier~~ | ✅ спринт 35: early 0.219 / mid 0.143 / late 0.103 — ранняя игра подтверждённо слабейшая; артефакт+Prometheus+Telegram |
| A8 | ~~Про-эталон~~ | ✅ спринт 34: JSON-путь /proMatches + реплейный про-коллектор; гейт сам переключился на про-эталон |
| A9 | ~~Даунвейт старого патча после баланс-патча~~ | ✅ спринт 52: колонка patch в витрине (миграция 010, оба пути сбора), вес строки ×PATCH_OLD_WEIGHT^возраст (дефолт 0.4, пол 0.1); patch=0 (старые строки) без штрафа; метрики считаются без даунвейта |
| A10 | Roshan/aegis, buyback, hero-фичи, Optuna-тюнинг | Поглощён треком F (docs/ML-PLAN.md): объективы — F2, hero-фичи — F3/F8, Optuna — F7 |
| A11 | ~~Гиперпараметры малых данных~~ | ✅ спринт 33 |

## Трек B — релиз Win Probability

Критерии из анализа (§5.1): порог — 1 000 матчей текущего патча (пройден),
плюс два качественных гейта.

| # | Критерий | Статус |
|---|---|---|
| B1 | OOF-Brier ≤ 0.18 | ✅ **0.1429** (спринт 33) |
| B2 | Brier про-эталон ≤ 0.20 на ≥ 30 pro-матчах | ✅ **0.150 на 36 матчах** (спринт 34) |
| B3 | ~~Beta-метка WP-кривой на фронте~~ | ✅ спринт 40: бейдж с тултипом (Brier, охват данных) |
| B4 | Публичный релиз WP | **Критерии B1+B2 выполнены** — решение за владельцем; после патчей — 200–300 матчей новой меты |

## Трек C — крупные фичи спеки (Фаза 3–4)

| # | Работа | Заметки |
|---|---|---|
| C1 | ~~Similarity Engine~~ | ✅ спринт 36: gRPC :50052, индекс 2800+ матчей, эмбеддинг траектория+мета+составы |
| C2 | ~~Draft Engine бейзлайн~~ | ✅ спринт 37: gRPC :50053, частоты со сглаживанием (GNN — при 10K+) |
| C3 | ~~LLM-Coach каркас~~ | ✅ спринт 38: gRPC :50054, шаблонный план + опциональный LLM-слой env-ключом |
| C4 | ~~Feature Store — онлайн-слой (Redis)~~ | ✅ спринт 44: gRPC :50055 (Write/GetOnlineFeatures), писатель — extractor (view match_timeline), TTL 7 дней |
| C5 | ~~Laning/Error-модели поверх baseline ΔWP~~ | ✅ спринт 48: Death-Risk (AUC 0.79) заменила эвристический SI; ✅ спринт 50: Laning-модель (AUC 0.89, brier_skill 0.47) — P(линия выиграна к 10-й) по игре первых 5 минут вместо сигмоид-эвристики |
| C6 | ~~Frontend-развитие~~ | ✅ спринты 40–42: SHAP-драйверы, драфт-симулятор (/draft), карта смертей (pos в ошибках отчёта + SVG-миникарта) |
| C7 | ~~Единая система координат + настоящая карта подложкой~~ | ✅ спринт 139: `libs/dota_map.py` — одно место для границ и переводов вместо трёх несогласованных определений. Неправым оказалось 8000 в mapcells: это половина ДИАГОНАЛИ, попавшая в роль половины стороны; арифметика радиуса варда (1600 юнитов = 12.5 клетки ⇒ 128 юнитов на клетку ⇒ ±8192) подтвердила это на своих данных. Карта 7.41 картинкой под всеми картами матча; центр подложки ИЗМЕРЕН по симметрии карты (регистрация по градиенту: по яркости не работает — Radiant светлый, Dire тёмный), масштаб — единственный неизвестный, подбирается ползунком в режиме калибровки |
| C8 | ~~Фарм коров + карта пятнами~~ | ✅ спринт 140: масштаб подложки подобран по живым данным (0.975). Новый вид `farm_core` — безопасный фарм ТОЛЬКО позиций 1–3 (ранг по добиткам, не по net worth: он растёт от убийств, и накормленный пос-4 обгонял бы честно фармящий оффлейн). Добавлен рядом с `farm`, а не вместо: переопределение сделало бы одно имя означающим разное в зависимости от даты разбора. Непрерывные виды рисуются пятнами (гаусс + шкала), событийные остались клетками — размазать одиночный вард значило бы придумать плавность там, где её нет. У матчей, разобранных до спринта 140, `farm_core` отсутствует; появляется при переразборе реплея |
| C9 | ~~Фарм измеряется, а не угадывается~~ | ✅ спринт 141: фарм определялся как «герой жив и рядом нет врага» — признак безопасности вместо измерения. На ФОНТАНЕ он срабатывал безотказно (воскрес, врагов нет), и карты светились там, где не фармят никогда; увидеть это стало можно только после настоящей подложки. Теперь фарм — интервалы, в которые рос счётчик добиток: позиции и экономика пишутся парсером в одном цикле раз в 300 тиков, поэтому game_time у них совпадает тик в тик. Бэкфилл расширен на пересчёт обоих видов фарма с гашением исчезнувших клеток нулём (без единого DELETE) |
| C10 | Ползунок времени вместо трёх фаз | Фазы (< 10 мин / 10–25 / > 25) — грубая нарезка: маршрут фарма меняется поминутно, а «мид» склеивает двадцатую минуту с двадцать четвёртой. Нужен поминутный агрегат (заменяет phase, фазы и «вся игра» получаются суммированием) и ползунок с окном на фронте |
| C11 | Модель эталонных маршрутов фарма | По многим матчам одного героя: отбросить те, где net worth набран убийствами, оставить фармовые, отранжировать по скорости и построить поминутный маршрут «лагерь → линия → лагерь» с ожидаемым GPM. Опирается на C10 |
| C12 | Оценка вардов и «лучшие варды патча» | Ценность = сколько прожил × сколько ПОЛЕЗНОЙ площади показывал. Вард под своим фонтаном не должен побеждать за одно долгожительство. Считать по MatchMapCells (присутствие противника в накрытых клетках), «спорность» — отдельным сигналом. Показ — на подложке из C7 |

## Трек D — MLOps и эксплуатация

| # | Работа | Заметки |
|---|---|---|
| D1 | ~~Продовый MLflow~~ | ✅ спринт 45: backend-store — postgres (БД mlflow, миграция 004), артефакты через --serve-artifacts; включение — REGISTRY_BACKEND=mlflow |
| D2 | ~~Авточистка версий реестра~~ | ✅ спринт 39: cleanup() — последние N (REGISTRY_KEEP_LAST=10) + все продвигавшиеся (история промоушенов) |
| D3 | ~~CI: тесты всех Python-сервисов~~ | ✅ спринт 39: + data-collector, similarity, draft, coach (report-generator уже был) |
| D4 | ~~Runbooks инцидентов~~ | ✅ спринт 39: docs/runbooks.md + алерт «витрина не растёт» (DATASET_STALL_ALERT_H) + метрика/лог 429 OpenDota |
| D5 | ~~Нагрузочные тесты NFR-PERF/SCAL~~ | ✅ спринт 56: scripts/loadtest.py (make loadtest) — все 5 NFR прошли: PERF-01 3.5с (порог 10), PERF-02 p95 8мс (2с), PERF-03 p95 10/p99 17мс (300/800), PERF-04 4136 реплеев/ч (2000), SCAL-01 отсечение партиций на 5М строк |
| D6 | ~~Security review (SAST/SCA, секреты, GDPR)~~ | ✅ спринт 57: docs/security-review.md + scripts/security-scan.sh (make security-scan, джоба CI). Секретов в дереве и истории нет; Go-модули обновлены (x/net, x/text, x/crypto, pgx); pip-audit/npm audit чисты. Всё, что лечилось кодом, закрыто: JWT/RBAC+TLS+GDPR — спринт 58, stdlib Go — спринт 68 |
| D7 | Соли реплеев у Game Coordinator вместо квоты OpenDota | 🚧 спринт 171: соль знает ТОЛЬКО GC, и сегодня за каждую платится вызов `/matches/{id}` из 2000 в сутки на весь проект — платный тариф OpenDota владельцу недоступен (нужна карта Visa/Mastercard). Замеры 2026-09-01 на живом аккаунте: соли отдаются и РАБОТАЮТ (10 из 10 проверенных скачались с CDN, ответ 206). Бюджет аккаунта оказался НАКОПИТЕЛЬНЫМ, а не частотным: первые 200 запросов прошли подряд (накопитель, полный за месяцы простоя), дальше около 0.3 соли в минуту, а прогон с паузой в 10 секунд — вдесятеро медленнее удачного — сломался на первом же запросе. Значит «спрашивать помедленнее» не помогает, помогает спрашивать МЕНЬШЕ; порядок величины 200–400 в сутки. Для проекта этого хватает с запасом: узкое место реплейного пути не соли, а трафик (58 МиБ на матч). Сделано: таблица `ReplaySalts` (миграция 014, соль BIGINT — fixed32 БЕЗ знака в INT не помещается) и наполнитель `scripts/gc-node/gc-salts.mjs` (`make gc-salts`), берущий маленькими порциями по расписанию. Молчание GC — штатный конец («бюджет на сегодня»), а не алерт: ежесуточная ложная тревога учит не читать канал (урок спринта 163). Спринт 172: читающая половина — `collector/salts.py`, соль от GC как ЗАПАСНОЙ путь. Поправка к формулировке спринта 171: выигрыш НЕ в экономии квоты. Ответ `/matches/{id}` несёт кроме адреса ранги десяти игроков, а `/players/{id}` даёт один ранг за вызов — менять оплаченный ответ на свою соль значит обменять десять рангов на один. Выигрыш в независимости: свежий матч не теряется в `no_salt`, пока OpenDota его не разобрала; исчерпанная квота больше не останавливает выдачу (цикл перестаёт ходить к OpenDota, но продолжает отдавать матчи с солью, и спать уходит только если соли не спасли никого); кандидат не получает штрафную попытку за наш голод. Мутационная проверка 13 из 13. Спринт 173: расписание (`scripts/gc-salts.sh` ежечасно в :17, ставит `vps-bootstrap.sh`) и учёт бюджета в той же `ApiBudget` под своим `api` — считаются ЗАПРОСЫ, а не добытые соли: единицу съедает сам вопрос, и учёт по успехам занижал бы расход тем сильнее, чем хуже идут дела. Потолок `GC_SALTS_DAILY_MAX` (400) — предохранитель: после долгого простоя накопитель отдаёт сотнями. Обёртка существует ради одного различения: молчит на удачной порции, выбранном потолке, молчании GC и машине без токена (все четыре наступают штатно); зовёт, когда соли не будет ни сегодня, ни завтра (нет node, не поднялась сессия, лежит база). Установка ставит nodejs; по дороге выяснилось, что пакет apt и команда зовутся по-разному, поэтому `HOST_TOOLS` стал парами «команда:пакет». Мутационная проверка 14 из 14 |
| D8 | ~~Гейт продвижения перестал застревать молча~~ | ✅ спринт 174: 2026-09-01 в логе нашлись четыре цикла подряд «NOT promoted» при production от 25 августа — и ни метрики, ни канал об этом не говорили: гейт отклоняет слабых кандидатов, это его работа, и со стороны застой неотличим от исправной работы. Три изменения. (1) Отказ называет ОБЕ оценки: раньше стояла одна цифра по про-эталону, и из неё не следовало, стал ли кандидат хуже вообще или только на двух десятках про-матчей — а это разные диагнозы (чинить обучение или признать эталон малым). Решает по-прежнему первый holdout, второй справочный и стоит одного лишнего предсказания; решающая печатается первой. (2) Метрики `wp_production_age_hours`, `wp_gate_rejections_consecutive`, `wp_gate_frozen` и алерт по СОЧЕТАНИЮ признаков (отказы И возраст > `GATE_FROZEN_ALERT_H`=72ч): каждый по отдельности законен, и алерт по одному был бы ложной тревогой. Возраст переживает перезапуск, счётчик — нет, поэтому решает возраст. (3) Порог гейта сведён с `ablation.MIN_EFFECT`: было 0.0005 против 0.001, то есть гейт был строже той меры, которой мы сами измеряем пользу. Снят кусок недостижимой защиты (`& ~pro` в `eval_holdouts`). Мутационная проверка 16 из 16, из них 4 теста пришлось усилить — в том числе проверка часового пояса, которая в UTC-контейнере не проверяла ничего |

## Трек F — максимальное усиление модели (план: docs/ML-PLAN.md)

Анализ 2026-07-26: главный резерв — ранняя игра (Brier early 0.186 против
0.077 mid); крупнейший источник — уже скачиваемый JSON, из которого
используется ~10% полей. Ни один спринт трека не тратит квоту OpenDota.

| # | Спринт | Содержание | Статус |
|---|---|---|---|
| F1 | 60 | Фундамент: сырой JSON в MinIO (бэкфилл навсегда), таблицы MatchDraft + MatchEvents, BUYBACK из ядра | ✅ |
| F2 | 61 | Объективы в WP: roshan_diff, aegis_alive, buybacks_diff, first_blood | ✅ реализовано, **эффект не измерен** (§8.6 ML-PLAN) |
| F3 | 62 | Draft Prior Model (P(win\|составы)) + каскад: draft_prior как фича WP | ❌ **фича УДАЛЕНА** спринтом 134. Абляция на 3420 матчах: Δ = +0.00001 ± 0.00008 — плоско до пятого знака при пороге значимости 0.001. Сама модель (AUC 0.585) остаётся: у неё свой продукт — DraftService |
| F4 | 63 | Предметы: item_value_diff, key_items_diff | ✅ реализовано, эффект не измерен |
| F5 | 64 | Вижн и руны: obs/sen wards diff, runes_diff | ✅ реализовано, эффект не измерен |
| F6 | 65 | Нейтралки и уровни: neutral_tier_diff, levels_diff | ✅ реализовано, эффект не измерен |
| F7 | 66 | Optuna-тюнинг, монотонные ограничения новых фич | ✅ тюнер есть; выигрыш 0.00053 < порога 0.002 → параметры НЕ применены |
| F8 | 67 | Hero-эмбеддинги (гейт 20К+ матчей) | ⏸ ждёт данных: 2355 матчей из 20000. Разблокируется платным ключом OpenDota (спринт 183) — владелец подтвердил намерение добрать объём. До тех пор роль «модель знает о героях» играют атрибуты состава (спринт 187), которые ревизия 190 признала полезными |

**Честный итог трека F** (подробности — `docs/ML-PLAN.md` §8): весь код
написан, протестирован и проверен сквозняком на живом матче (188 событий
из одного JSON), FEATURES выросли 10→22, WP v0.8.0. Но **измеримого
улучшения метрики сегодня нет**: F2–F6 извлекаются в момент сбора, а для
2635 уже собранных матчей новые колонки — NaN (сырой JSON тогда не
сохранялся; ровно ради этого сделан F1). Draft prior измерить удалось —
и он не помог (early 0.2193 → 0.2188 при σ≈0.003, про-эталон 0.1083 →
0.1093, то есть хуже). Ревизия — после накопления ~2–3 тысяч новых
матчей с полными сигналами; правило §6 плана («фича, не двинувшая
метрику, выкидывается») к ним ещё будет применено.

**Ревизия проведена (спринт 190), правило §6 применено впервые.** Волны
184–187 добавили 22 фичи; абляция на 2355 матчах (база Brier 0.1338)
оставила **семь**. Роли героев оказались не бесполезны, а ВРЕДНЫ
(Δ −0.00227); драки и «фарм» — неотличимы от шума; атрибуты состава
сработали (+0.00255, на ранней фазе +0.00399) — единственное, что из
двадцати двух доказало пользу. Фич в модели 63 → 48. Подробности и
методический урок про разбиение семейств — ML-PLAN §10.

Ревизия же вскрыла, что `make test` гонял только Go: сторожевой тест
`test_wp_features_sync` горел красным со спринта 84, и ни одна фича волн
185–187 не доехала до report-generator. Появилась цель `make pytest` по
всем `apps/*`, от которой теперь зависит `make test`.

**Спринт 195 — частичные отчёты по JSON-матчам.** Отчётов в базе 520 при
3091 собранном матче: разбор получал только реплейный путь, потому что
JSON-путь ничего не публиковал — осознанное решение времён, когда
продуктом был датасет, а не сайт. Теперь `timeline_runner` публикует
`features.calculated` в тот же топик, что и feature-extractor (потребитель
не должен знать, каким путём приехал матч), с `player_rows: 0` в конверте.
Составы у JSON-матча берутся из `MatchDraft` — единственной таблицы,
которую заполняют оба пути, — целиком или никак, и только когда
поигрокового разреза нет. В разбор добавлен блок `available`
(`players`/`positions`/`kills`/`heatmaps`): сайт обязан отличать «данных
нет» от «данные нулевые», а флаг `partial` для этого не годился — он
истинен всегда. Недоставка события здесь, в отличие от 194-го, сбор не
отменяет: строки витрины уже записаны и отчёт из них восстановим, тогда
как на реплейном пути терялся единственный след.

**Спринт 194 — тихие потери конвейера.** Три дефекта из разбора аварии
3–6 сентября: коллектор не проверял доставку события и помечал матч
собранным при потерянном сообщении (матч выпадал навсегда); потерянные за
трое суток матчи нечем было вернуть; дубликат разбора выглядел точно так
же, как настоящая потеря реплея. Сделано: подтверждённая публикация с
колбэком доставки и без повторной закачки уже сохранённого файла;
`tools/republish_orphans.py` (сироты ищутся пересечением «реплей есть» в
Postgres и «разбора нет» в ClickHouse, tier берётся из витрины, а не
выдумывается); различение дубликата от потери по коду ошибки S3 и наличию
событий в витрине, с отдельным счётчиком `replays_duplicate_total`.
Заглушки тестов приведены к настоящему контракту клиентов — по правилу
из 193-го.

**Спринт 193 — Kafka без сохранности и сторож без содержания.** Живой
отказ: 3 сентября пересоздание контейнеров стёрло ВСЕ семь топиков Kafka,
и трое суток продюсеры писали в никуда молча (автосоздание выключено
намеренно). Причина старше отказа — том был смонтирован в
`/var/lib/kafka/data`, а образ писал в `/tmp/kafka-logs`: `KAFKA_LOG_DIRS`
никто не задал, и том стоял пустым с даты сборки образа. Сохранности не
было НИКОГДА. Второй находкой: 🔴 в Telegram приходил без единой причины
всё это время — `doctor` печатает FAIL с цветом, а `heartbeat` искал
`^\s+FAIL`, и совпадений не было ни разу; тест на это был зелёным,
потому что его заглушка не красила вывод. Сделано: `KAFKA_LOG_DIRS`,
статический тест «каталог данных = точка монтирования», проверка «том не
пуст» в doctor (работает на любом образе), снятие цвета в heartbeat и
честная заглушка в тесте. Из четырёх дефектов подряд выведено правило
проверок — см. HANDOFF, «вход должен быть боевым».

**Спринт 192 — публичный API для сайта.** `GET /api/v1/matches` из
заглушки стал настоящим: карточка матча (стороны, счёт, длительность,
патч, уровень, пики, финальная WP), пагинация курсором по `match_id`,
фильтры `patch`/`tier` и поиск по герою или номеру матча. Данные —
таблица `MatchSummaries` (миграция 015), которую заполняет
report-generator в момент генерации: шлюз в ClickHouse по-прежнему не
ходит. Публичный слой — отдельный слушатель :8081, где зарегистрированы
ровно пять маршрутов, а всё остальное отвечает 404 по построению; CORS по
точному перечню доменов, ETag и Cache-Control на публичных GET, trace_id
в теле ошибки. TLS, домен и вывод наружу — 193-й.

**Спринт 191 — авария, которую та же ревизия и нашла.** Отчёты не
производились 29 часов (2.09 07:00 — 3.09 12:01) и об этом не узнал
никто. Сторожа были на состав фич, на гейт, на темп сбора — и ни одного
на то, производится ли продукт. Сделано: сторож за долей неудач
генерации с точным диагнозом при рассинхроне фич, автоматический подхват
новой версии модели в ml-service (`ModelSlot` кэшировал первую
загруженную НАВСЕГДА), отправка в Telegram вынесена в `libs/`, логи
контейнеров переведены на journald — раньше они умирали вместе с
контейнером, и причину рестарта установить уже не удалось. `make pytest`
расширен на `libs/` и `scripts/`: дыра была шире, чем виделось из 190-го.
Подробности — HANDOFF, спринт 191.

## Трек G — когнитивные признаки (план: docs/COGNITION-PLAN.md)

Волна, выросшая из документа владельца «DOTA COGNITION». Философия
переведена в измеримые величины: производные и скорости (G1), окна силы
и доступность ультов (G2), темп как оценщик (G3), энтропия позиций
противника (G4), ценность информации (G5), таксономия ошибок (G6),
условная ценность событий (G7), проверка иерархии целей (G8).

Рамка сужена намеренно: играющий агент (RL/self-play) вне досягаемости
на два домашних ПК, а «открытие стратегий, которых люди не придумали»
невозможно из наблюдательных данных в принципе. Оба пункта вычеркнуты.

Правило волны: каждое понятие — одна фича или модель, проходит
`make ml-ablation`, не давшая улучшения УДАЛЯЕТСЯ. Прямой вывод из трека
F, где двенадцать фич не дали измеримого эффекта.

| ID | Что | Статус |
|----|-----|--------|
| G1 | Производные и скорости (окна 1/3/5 мин) — ретроактивно на всём датасете | 🔬 спринт 131: 15 фич считаются оконными функциями ClickHouse в `match_rows_sql`, миграции и пересбора не потребовалось; вердикт — за `make ml-ablation` на живом датасете |
| G2 | Окна силы и доступность ультов из MatchHeroTimings (миграция 021 построена ради них) | ⏳ |
| G3 | Темп как измеримая величина из MatchFights | ⏳ |
| G4 | Энтропия позиций противника — единственное, чего нет у STRATZ/OpenDota | ⏳ |
| G5 | Ценность информации через снижение энтропии | ⏳ зависит от G4 |
| G6 | Таксономия ошибок (темп/пространство/информация/экономика/стратегия) | ⏳ |
| G7 | Условная ценность событий вместо каузальной цепочки | ⏳ исследование |
| G8 | Проверка иерархии целей документа | ⏳ вердикт |

## Трек E — окружения

| # | Работа | Заметки |
|---|---|---|
| E1 | ~~Автозапуск стека на локалке (Windows)~~ | ✅ спринт 55: scripts/autostart-install.ps1 — задачи Manta-Recover (при входе: Docker Desktop + ожидание демона + make recover) и Manta-Backup (ежедневно); scripts/backup.sh — слепок с ротацией KEEP_DAYS и Telegram-алертом сбоя |
| E2 | ~~Синхронизация облако↔локалка~~ | ✅ спринт 43: make dataset-export / dataset-import (идемпотентно). 2026-07-19: облачный сбор ОСТАНОВЛЕН решением владельца — обучение и сбор только на локалке; финальный слепок облака передан (4097 матчей). Спринт 156: перенос Postgres перестал быть двумя строками кода — списки таблиц вынесены в `scripts/lib/dataset-tables.sh` (один на обмен, учения и отчёт) и сверяются с миграциями ОБЕИХ баз. В слепок вошли `playerranks`, `replaycandidates`, `parkedreplays`: кэш рангов стоит квоты OpenDota и копится неделями, а не переносился — свежая машина получала полные витрины и пустую очередь кандидатов. Архив несёт свой список колонок, поэтому схемы машин могут расходиться; слепки старого формата читаются по числу полей в CSV. Спринт 157: `vps-bootstrap.sh` ставит `make` и `rclone` — установка заводила расписание (обмен 04:00, оффсайт 03:30) для rclone, которого не ставила, и печатала напутствие командой `make`, которой на Ubuntu-сервере нет; тест связывает предписанные команды с тем, что установка на самом деле кладёт на машину. Спринт 163: **одиночная машина — законная конфигурация, а не недонастроенная пара.** Обмен ставится в расписание только при заданных `MANTA_PEER_HOSTS`, а без соседей `peer-sync.sh` завершается спокойно вместо красного алерта: ежесуточная ложная тревога учит не читать канал. Расписание сверяется по СОДЕРЖИМОМУ (чужие строки crontab сохраняются), поэтому правки доезжают и до уже настроенных машин — три коммита-починки спринтов 159–161 случились именно из-за обратного |
| E3 | ~~Параллельный сбор на 2+ машинах~~ | ✅ шардирование `match_id % N` (`COLLECTOR_SHARD_COUNT`/`COLLECTOR_SHARD_ID`, класс `Shard`) — разные IP, разные квоты OpenDota, пересечений нет, dataset-import конфликт-фри |
| E6 | ~~`manta up` — запуск одной командой~~ | ✅ спринт 77: scripts/manta (up/down/doctor/backup/restore). `manta up` = git pull → recover → восстановление бэкапа ТОЛЬКО на пустой витрине (порог MANTA_RESTORE_THRESHOLD). Свежая машина после переустановки поднимается и подтягивает данные одной командой |
| E5 | ~~Оффсайт-бэкап в облако~~ | ✅ спринт 76: backup.sh после локального слепка заливает tar через rclone (`MANTA_CLOUD_REMOTE`, по умолчанию выкл.) — Google Drive/S3/что настроено; та же ротация KEEP_DAYS, та же задача Manta-Backup. PII: рекомендован crypt-ремоут (клиентское шифрование). Локальный бэкап на диске машины умирал вместе с ней — оффсайт это чинит |
| E8 | ~~Второй источник деталей (STRATZ)~~ | ✅ спринт 79: `stratz-timeline`/`-pro` — кандидаты дешёвым листингом OpenDota, поминутные ряды из GraphQL STRATZ. Потолок деталей ~2000 → ~10000/сутки (Default-токен). Патчи переводятся по имени версии (gameVersionId ≠ patch id OpenDota); позиции и трек F остаются NaN; строки помечены `feature_version=stratz-graphql@1`. Включается наличием `STRATZ_API_TOKEN` в env-файле |
| E7 | ~~Курсор коллектора не блокируется несобираемым матчем~~ | ✅ спринт 78: инцидент 2026-07-31 — реплейный путь стоял 82ч при живых процессах и лаге Kafka 0. Курсор двигался только после успешной публикации, а `fetch_new` отдаёт кандидатов старыми вперёд → битый bz2 (или дубликат из общей CollectedMatches) возвращался первым каждый цикл. `_advance_cursor()` сдвигает курсор через пропущенный матч; `PermanentDownloadError` пропускается сразу, временные сбои — после 3 попыток |
| E4 | ~~Темп сбора под квоту~~ | ✅ 2026-07-26: `TIMELINE_LIMIT=14`, `PRO_TIMELINE_LIMIT=4`, `OPENDOTA_LIMIT=2` в env-файле — ~1780 вызовов/сутки на машину (бюджет ~1660–2100), выхлоп ~840 матчей/сутки, на двух машинах ~1700. Раскладка бюджета и контроль 429 — `docs/HANDOFF.md`, «Темп сбора и бюджет квоты» |

---

## Предлагаемый порядок ближайших спринтов

Спринт 49 (надёжность) ✅: recover идемпотентно прогоняет `make topics` +
миграции (журнал `SchemaMigrations` в PG), `make doctor` — health-check по
ДАННЫМ, Telegram-алерт «реплейный путь стоит», логи — `~/manta-logs`.
Спринт 50 (Laning-модель) ✅: C5 закрыт целиком — обе модели обучаемые.
Спринт 51 ✅: recover поднимает ВСЮ платформу одной командой — включая
api-gateway (:8080), веб-интерфейс (:5173) и дашборд (:9107).
Спринт 52 (A9) ✅: patch в витрине + даунвейт старого патча при обучении.
Спринты 53–54 ✅ (эксплуатация): recover ставит python-зависимости;
журнал CH-миграций — 002 больше не стирает ReplayEvents каждый recover.
Спринт 55 (E1) ✅: автозапуск на Windows + ежедневный бэкап с ротацией.
Спринты 56–57 (D5/D6) ✅: нагрузочные тесты — все NFR-PERF/SCAL в пределах
порогов; security review — критичных незакрытых находок нет.
Спринт 58 ✅: JWT/RBAC (RS256, denylist, JWKS, матрица ролей Гл. 9.3.2),
TLS 1.3 в gateway, GDPR-эндпоинты export/erasure — сняты три из гейтов B4.

Спринт 59 ✅: аудит источников и план максимального усиления модели —
docs/ML-PLAN.md, трек F (8 спринтов, F1–F8). A10 поглощён треком F.
Спринты 60–66 (F1–F7) ✅ выполнены разом по решению владельца
(«выполни сразу все спринты, если для некоторых не хватает матчей —
пока не выполняй»): 12 новых поминутных фич + draft prior + Optuna.
F8 пропущен по гейту данных. Итоги — честные, см. выше и ML-PLAN §8.

Трек D закрыт целиком. Порядок дальше:

1. **Накопить данные и провести ревизию трека F** — это теперь главный
   пункт. Ничего нового кодить не нужно: нужны ~2–3 тысячи матчей,
   собранных уже с извлечением сигналов, затем переобучение и фазовое
   сравнение. Фичи без эффекта — выкинуть.
   При темпе сбора от 2026-07-26 (E4, ~1700 матчей/сутки с двух машин)
   это **2–3 суток**, а не месяцы — ради этого темп и поднимали.
   Первые сутки после переключения следить за 429: перебор квоты
   означает сон коллектора до 00:00 UTC и в сумме МЕНЬШЕ матчей.
   **Инструмент ревизии готов** (спринт 69): `make ml-ablation` —
   переобучение без каждой группы фич + парный бутстрап, вердикт по трём
   порогам (значимость, практический размер эффекта, покрытие). Отличает
   «бесполезна» от «неизмерима» — см. ML-PLAN §9.
   **Спринт 132 — инструмент чинил сам себя перед прогоном.** Δ мерился
   по ВСЕЙ валидации, включая строки, где фичи нет и обе модели видели
   одно и то же: реальный эффект делился на долю покрытия, и тем сильнее,
   чем реже фича. Фича трека F с покрытием 30% и честным эффектом 0.003
   давала 0.001 и попадала в вердикт «практической пользы нет» — то есть
   ревизия старым инструментом выкинула бы работающие фичи, ради проверки
   которых он и написан. Теперь вердикт выносится по Δ НА ПОДВЫБОРКЕ, где
   фича наблюдаема, а Δ по всей валидации печатается рядом как масштаб
   продуктового эффекта. Добавлены разбивка Δ по фазам игры (объективы
   живут в поздней, производные — в ранней; агрегат это маскировал) и
   число МАТЧЕЙ, несущих сигнал, — доля строк на вопрос «сколько ещё
   копить» не отвечает.
   Попутно там же найден **мёртвый тест**: скриптовая правка спринтов
   60–66 вклинила хелпер `_pad` между сигнатурой `test_autotrain_thresholds`
   и её телом, тело уехало в недостижимый код после `return`, а тест
   сжался до докстринга и импорта. Семьдесят спринтов он был зелёным и не
   проверял ничего — а сторожил пороги АВТОМАТИЧЕСКОГО переобучения.
   Восстановлен; добавлен сканер `test_no_dead_tests.py`, который ищет
   недостижимый код и пустые тесты по всему монорепо (pytest и ruff такое
   не видят: синтаксис корректен, имена определены).

   **Прогон состоялся 2026-08-07**: 5391 матч, 183 981 строка, базовый
   Brier валидации **0.1371**. Результат по группам (Δ на подвыборке, где
   фича есть):

   | группа | покрытие | Δ где есть | σ | вердикт |
   |---|---|---|---|---|
   | база_экономика | 100% | **+0.00513** | 0.00144 | ПОЛЕЗНА |
   | G1_производные | 97.1% | **+0.00162** | 0.00040 | ПОЛЕЗНА |
   | F6_нейтралки_уровни | 26.1% | +0.00126 | 0.00111 | шум |
   | F4_предметы | 26.1% | +0.00069 | 0.00027 | эффект ничтожен |
   | F2_объективы | 26.1% | −0.00003 | 0.00062 | шум |
   | F3_драфт_прайор | 66.6% | +0.00001 | 0.00008 | шум |
   | база_объекты | 59.4% | −0.00102 | 0.00065 | шум, знак отрицательный |
   | F5_вижн_руны | 26.1% | −0.00111 | 0.00069 | шум, знак отрицательный |

   Ни одна фича трека F гейт не прошла. Две — `F5_вижн_руны` и
   `база_объекты` — дали ОТРИЦАТЕЛЬНЫЙ Δ во всех трёх фазах: без них
   модель не «такая же», а чуть лучше.

   **Спринт 133 — вердикт «удалить» подпёрт числом матчей.** Четыре
   группы трека F имели покрытие 26.1% при пороге LOW_COVERAGE 25%: все
   четыре получили «кандидат на удаление», а полтора процентных пункта в
   другую сторону дали бы «вывод преждевременен». Решение об удалении
   фичи, стоившей спринта, не может держаться на такой щепке. Введён
   `MIN_VERDICT_MATCHES = 2000` — та же величина, что записана в условии
   этой задачи; доля строк отвечает на другой вопрос (фича может жить в
   5% строк, но присутствовать во всех матчах).

   **Открытый методический пробел.** Абляция «по одному» не отвечает на
   вопрос «какое подмножество достаточно»: у G1 сумма одиночных эффектов
   (0.00091) вдвое меньше эффекта семейства (0.00162), потому что окна —
   субституты. Нужна обратная процедура «оставить только X».

   **Спринт 134 — решение по числам матчей.** Замер: у F2/F4/F5/F6 сигнал
   в **1573 матчах**, у `F3_драфт_прайор` — в 3420, у `база_объекты` — в
   3119, у базовой экономики и G1 — во всех 5391.

   * `draft_prior` **УДАЛЁН** из модели. 3420 матчей — выше гейта, Δ =
     +0.00001 ± 0.00008, то есть плоско до пятого знака при пороге
     значимости 0.001. Сама Draft Prior Model остаётся: у неё свой
     продукт (DraftService), к WP она отношения больше не имеет. Заодно
     из обоих сервисов убран джойн `MatchDraft` — читать колонку, которую
     никто не использует, значит платить за неё на каждом обучении и
     каждом отчёте. MODEL_VERSION 0.8.0 → **0.9.0** (спринт 131 добавил
     15 фич и версию не поднял).
   * F2, F4, F5, F6 **НЕ удалены**: 1573 матча — ниже гейта в 2000.
     «Эффекта нет» здесь означает «не на чем проверить». Вернуться после
     роста реплейного датасета; при нынешнем темпе своей разбивки это
     дни, а не месяцы.
   * `база_объекты` (towers/rax/alive) — 3119 матчей, Δ = −0.00102,
     отрицательный в средней и поздней фазах. Гуртом не удаляется:
     группа смешанная (`towers_diff` есть на обоих путях сбора,
     `alive_diff` — только на реплейном), и минус может давать одна фича
     из трёх. Разбирается прогоном `--only`, добавленным тем же спринтом.

   **Спринт 137 — регресс ЗНАЧЕНИЙ фич.** До сих пор фичи проверялись
   как свойства: «знак в пользу Radiant», «вард живёт шесть минут»,
   «пустой лог даёт ноль». 37 таких тестов у коллектора, и все они
   переживают правку арифметики: подели стоимость выкупа на 12 вместо 13
   — каждое свойство останется верным, а числа модели поедут. Заведены
   три эталона, закрепляющие сами числа (`make golden-test`, обновление —
   `make signals-golden-update`):

   * **сигналы** (`apps/data-collector`) — синтетический матч из десяти
     игроков прогоняется через `signals.py`; закреплены все 14 поминутных
     фич на 13 минутах;
   * **производные** (`libs/wp_rates.py`) — 6 сценариев × 15 темпов. До
     этого численно были закреплены 4 производных из 15: остальные
     держались на цикле, который проверяет существование колонки, а не
     число в ней;
   * **вектор** (`row_to_features`) — 8 сценариев × 41 фича. Единственное
     место, где проверяется ПОРЯДОК: перестановка двух колонок ничего не
     ломает заметно, модель просто молча учится на перепутанных
     признаках. Расхождение FEATURES и сборки вектора уже стоило проекту
     нескольких суток простоя (спринты 90–91).

   Плюс мета-тест «фича без теста не проходит»: каждое имя из FEATURES
   обязано встречаться хотя бы в одном тестовом файле. Фикстуры при этом
   НЕ сканируются — голден вектора генерируется из FEATURES, и включение
   фикстур сделало бы мета-тест тождественно зелёным.

   Отдельно проверяется, что не выродилась сама фикстура: у каждой фичи
   в эталоне не меньше двух разных значений, и никакие две фичи не
   совпадают во всех сценариях. Вторая проверка сразу нашла шесть
   неразличимых фич (`roshan_diff`, `first_blood`, `key_items_diff` — все
   по 1.0), перестановку которых сравнение вектора пропустило бы.

   Мутационная проверка: 38 дефектов, **36 убито**. Два выживших
   доказуемо ненаблюдаемы (сдвиг всех уровней на +1 сокращается в
   разности пяти игроков против пяти; `abs(dt)` под охраной `dt > 0`).

   **Первый заход мутационного прогона соврал.** `cp` восстанавливал
   исходник в ту же секунду, в которую записывался `.pyc`, а Python
   сверяет mtime с точностью до секунды и считал кэш свежим — часть
   вердиктов относилась не к тому коду, который лежал на диске. Прогон
   переделан с отключённым байткодом и проверкой, что мутация ДОЕХАЛА до
   интерпретатора.

   **Спринт 137 — окно WSL можно закрыть на крестик.** Процессы стека
   запускаются через `nohup`, и SIGHUP им не страшен; убивал их сам WSL,
   который гасит виртуальную машину, когда закрывается последний сеанс
   дистрибутива. `scripts/wsl-anchor.sh` занимает сеанс, который никто не
   закрывает, и ставится задачей Планировщика `Manta-Anchor` (скрыто, при
   входе в систему и далее каждые 10 минут — самолечение после
   `wsl --shutdown` и выхода из сна). Вручную — `make wsl-anchor`,
   `make wsl-anchor-status`, `make wsl-anchor-stop`.

   Две ошибки, найденные тестами по дороге, обе — «проверка сообщала не о
   том»:

   * живость якоря определялась через `pgrep -f manta-anchor`, а он
     находит любой процесс, который метку просто УПОМИНАЕТ, — включая
     собственный `ps | grep` оператора и строку задачи Планировщика.
     Заменено на pidfile со сверкой `/proc/<pid>/cmdline`;
   * `stop` печатал «якорь снят», стирал pidfile и возвращал ноль, а
     якорь продолжал жить. Тело было `trap ... TERM; while true; do
     sleep 3600; done`, а bash не выполняет обработчик, пока не
     завершится текущая команда переднего плана: SIGTERM ждал конца
     часового `sleep`. Каждый цикл «снял — поднял» оставлял лишний
     процесс, невидимый для `status`. Тело заменено на голый
     `exec -a manta-anchor sleep infinity`.

   Первая версия тестов дефект НЕ ловила: они подставляли короткий такт
   `sleep` через переменную окружения, и на нём обработчик успевал
   сработать. Проверялась конфигурация, в которой система не работает.
   Переменной больше нет.

   **Замер GC не запускался: сломан вход, а не пароль.** `make gc-probe
   ARGS=login` отвечал «Invalid password» на верный пароль — тот же
   пароль в браузере пускал. Сообщение печатает сама библиотека
   (`steam` 1.4.4, `client/__init__.py:664`), а логинится она устаревшим
   способом — пароль открытым полем в `EMsg.ClientLogon` (строка 548).
   Valve переработала аутентификацию в марте 2023: теперь нужен обмен
   через `CAuthentication` с refresh-токеном, а старый путь отвечает
   `InvalidPassword` при любом верном пароле.

   Проверено, а не предположено: 1.4.4 — последняя версия на PyPI, в
   master та же строка, `WebAuth` там же ходит на ретированный `dologin`.
   Протоколы нового входа в пакете уже лежат (`steammessages_auth_pb2`,
   поле `access_token` в `CMsgClientLogon`) — просто не связаны в рабочий
   вход. Вторая гипотеза отброшена замером на живой машине: длина пароля,
   которую видит скрипт, совпала с настоящей, то есть `. ~/manta-train.env`
   ничего не съел.

   `make gc-login-check` (`scripts/gc-node/login-check.mjs`) логинится
   НОВЫМ путём: `steam-user` 5.3.0 меняет пароль на refresh-токен и
   удаляет пароль из запроса (`components/09-logon.js:405`). **Живой
   прогон: вход прошёл.** Диагноз подтверждён — переписывать надо вход, а
   не пароль и не аккаунт. К GC проверка намеренно не подключается: пакет
   `dota2` для Node не обновлялся с 2022 года, и с ним при отказе было бы
   не отличить сломанный вход от устаревших протоколов GC.

   Первый живой прогон обнажил дефект самой проверки: вердикт печатался
   только внутри обработчика `accountLimitations`, а это событие пришло
   не всегда. Вход состоялся, ответ был получен и НЕ показан, скрипт
   висел до Ctrl+C. Выход развязан с необязательным событием.

   **Гибридный вход: Node добывает токен, замер остаётся питоновским.**
   Переписывать `gc-probe.py` целиком значило бы выбросить рабочее —
   режимы `details`/`bulk`, сквозную проверку соли на CDN, чтение
   очереди. Вместо этого разделение труда по единственной сломанной
   части: `make gc-token` меняет пароль на refresh-токен через Node, а
   `gc_token.py` входит этим токеном из Python. Работает это потому, что
   поле `access_token` в `CMsgClientLogon` у библиотеки есть — не хватало
   только токена и правильно собранного сообщения.

   Отличий от штатного `login()` ровно три, и каждое обязательно:
   `access_token` вместо `password`; `account_name` не передаётся вовсе;
   steamid в ЗАГОЛОВКЕ настоящий, а не пустой шаблон Individual/Public —
   берётся из самого токена, это JWT с полем `sub`.

   Просроченный токен отбивается ДО сети: Steam отвечает на него тем же
   `InvalidPassword`, что и на неверный пароль, и без ранней проверки
   диагноз пришлось бы восстанавливать по неразличимому симптому.
   Токен равносилен паролю: 0600, вне репозитория, не печатается и не
   попадает в тексты ошибок.

   Мутационная проверка гибрида: 18 дефектов, 18 убито (10 на входе по
   токену, 8 на его добыче). Три теста сборки сообщения требуют
   библиотеки `steam`, поэтому она добавлена в CI-джобу `scripts-test`:
   без неё они помечались бы пропущенными, и самая суть гибрида не
   проверялась бы вовсе, а файл выглядел зелёным.

   Мутационная проверка: 9 дефектов, 9 убито. По дороге снято два «на
   всякий случай» — флаг «уже печатали» и снятие таймера: оба оказались
   недостижимы (функция заканчивается `process.exit`), то есть были
   непроверяемым кодом. Тест на однократность вывода при этом остался, но
   переписан на ОПОЗДАВШЕЕ событие: на раннем свойство выполняется само
   собой, и тест не замечал снятия ни одной из защит.
2. **B4**: публичный релиз Win Probability. Технические критерии (B1+B2)
   выполнены; безопасность закрыта настолько, насколько это решается кодом
   монорепо (спринты 58, 68, 70). Оставшееся — решения прод-развёртывания,
   а не дефекты: сертификат от CA вместо самоподписанного, потоки входа
   Steam/email с refresh-ротацией, retention-политика отчётов —
   `docs/security-review.md` §4–6. Решение за владельцем.
3. ~~Обновить Go до 1.25.12+~~ ✅ спринт 68: `go 1.25.12` как ПОЛ версии
   в обоих go.mod, `golang:1.25` в Dockerfile'ах и CI. govulncheck: 26 и 22
   достижимых уязвимости → **0 и 0**. security-scan.sh считает находку
   govulncheck критичной (базовая линия — ноль), а не предупреждением.
   В обзоре безопасности не осталось находок, лечащихся кодом монорепо.
